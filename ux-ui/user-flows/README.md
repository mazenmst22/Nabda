# User flows

Journey and flow diagrams. Empty for now.

Expected: patient booking (search → doctor → slot → hold → confirm), patient cancel/reschedule, doctor encounter (record → transcribe → extract → review → approve), reception queue and walk-in, clinic admin onboarding, and the Pulse conversational booking path with its hand-off to the same booking engine.

Prefer text-based diagram sources (Mermaid, `.md`) over binary exports so flows stay diffable.
