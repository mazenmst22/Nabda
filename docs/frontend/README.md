# Frontend documentation

Not yet written.

Belongs here: App Router and route-group structure (`src/app/[locale]`), server state via TanStack Query and the rule against `useEffect` + `fetch`, Zustand's scope (transient UI state only), form conventions with React Hook Form + Zod, `next-intl` locale routing, the logical-properties-only CSS rule, and the per-route JavaScript budget enforced by `scripts/bundle-report.mjs`.
