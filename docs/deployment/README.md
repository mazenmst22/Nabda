# Deployment documentation

Not yet written.

Belongs here: environments and their configuration, the build and release process, the CI quality gate ([`../../.github/README.md`](../../.github/README.md)), performance budgets (`lighthouserc.cjs`, `scripts/bundle-report.mjs`), and observability — correlation IDs, error reporting and alerting.
