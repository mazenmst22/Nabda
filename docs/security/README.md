# Security documentation

Not yet written.

Belongs here: OIDC session handling and renewal (`src/lib/auth`), role and clinic claims, RBAC and route guards (`src/lib/rbac`), step-up authentication for privileged configuration and secret reveal, consent capture and revocation boundaries, the audit trail for sensitive reads and mutations, and PHI handling rules for logs, analytics and error reporting.

The backend obligations are listed in [`../backend/GAPS.md`](../backend/GAPS.md).
