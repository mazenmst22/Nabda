# Pulse (AI assistant) documentation

Not yet written.

Belongs here: Pulse's tool surface and booking permissions, the Arabic/English conversation model, safety boundaries (see `src/components/pulse/safety.ts`), the audio → transcript → prescription-extraction pipeline, job progress over SignalR, the `EXTRACTION_INVALID_JSON` and `PROVIDER_UNAVAILABLE` failure paths, and the clinician review step that gates every extracted prescription.
